import { MembershipsManager } from '@/components/dashboard/memberships/memberships-manager';

export const dynamic = 'force-dynamic';

export default function MembershipsPage() {
  return <MembershipsManager />;
}